<Page
    x:Class="_7._11CameraCaptureUI.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:_7._11CameraCaptureUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        
        <StackPanel Margin="10,40,10,10" Grid.Row="0" Orientation="Horizontal">
            <Button Content="Get Image" Click="Button_Click"/>
            <Image x:Name="ImageSource" Width="200" Height="200"/>
        </StackPanel>

        <StackPanel Margin="10,10,10,10" Grid.Row="1" Orientation="Horizontal">
            <Button Content="Get Video" Click="Button_Click_1"/>
            <MediaElement x:Name="mediaElement" Width="320" Height="240" AreTransportControlsEnabled="True"/>
        </StackPanel>
    </Grid>
</Page>
